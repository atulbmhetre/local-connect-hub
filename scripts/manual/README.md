# Manual verification scripts

Operator-run diagnostics against a live environment (typically TEST).

These are **not** part of the automated Playwright or Vitest suite.
Run them explicitly when you need a hands-on discoverability check.
